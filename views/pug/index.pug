extends ../layout

block content
  .container
    .row
      each item in movies
        h2 
        input(type=type, value='Hello #{item.title}')
        a(href='#{item.poster}')  asdf

        #{item.poster} #{item.title}
        .col-md-2
          .thumbnail
            a(href="/movie/#{item._id}")
              img(src="#{item.poster}", alt="#{item.title}")
            .caption
              h3 #{item.title}
              p: a.btn.btn-primary(href="/movie/#{item._id}", role="button") 观看